# 蒸馏功能数据流检查报告

**检查日期**: 2026-01-18  
**检查范围**: 关键词蒸馏页面、蒸馏结果页面、数据库使用

---

## 一、数据流程图

```
用户输入关键词
    ↓
[关键词蒸馏页面] DistillationPage.tsx
    ↓
1. 调用服务器 API: POST /api/distillation
    ↓
[服务器端] server/src/routes/distillation.ts
    ↓
2. AI 生成话题（DeepSeek/Gemini）
    ↓
3. 服务器返回话题列表
    ↓
[Windows 端] DistillationPage.tsx
    ↓
4. 保存蒸馏记录到本地数据库（geo_windows）
    ↓
5. 保存话题到本地数据库（geo_windows）
    ↓
6. 导航到蒸馏结果页面
    ↓
[蒸馏结果页面] DistillationResultsPage.tsx
    ↓
7. 从本地数据库读取话题列表
```

---

## 二、数据库使用检查

### ✅ 正确的数据库使用

| 步骤 | 操作 | 数据库 | 位置 | 状态 |
|------|------|--------|------|------|
| 1 | AI 生成话题 | 服务器（geo_system） | `server/src/routes/distillation.ts` | ✅ 正确 |
| 2 | 保存蒸馏记录 | Windows 端（geo_windows） | `DistillationPage.tsx` L238-246 | ✅ 正确 |
| 3 | 保存话题 | Windows 端（geo_windows） | `DistillationPage.tsx` L249-263 | ✅ 正确 |
| 4 | 读取蒸馏历史 | Windows 端（geo_windows） | `DistillationPage.tsx` L30-36 | ✅ 正确 |
| 5 | 读取蒸馏结果 | Windows 端（geo_windows） | `DistillationResultsPage.tsx` L62-68 | ✅ 正确 |
| 6 | 读取关键词列表 | Windows 端（geo_windows） | `DistillationResultsPage.tsx` L72-78 | ✅ 正确 |

---

## 三、关键代码检查

### 3.1 关键词蒸馏页面（DistillationPage.tsx）

#### ✅ 步骤 1: 调用服务器 API 执行蒸馏

```typescript
// L227-229: 调用服务器 API
const response = await apiClient.post('/distillation', { keyword: keyword.trim() });
const questions = response.data.questions || [];
```

**检查结果**: ✅ 正确
- 调用服务器 API `/api/distillation`
- 服务器使用 AI 生成话题
- 返回话题列表

#### ✅ 步骤 2: 保存蒸馏记录到本地数据库

```typescript
// L233-246: 保存蒸馏记录
const createResult = await localDistillationApi.create({
  keyword: keyword.trim(),
  topic_count: count,
  provider: response.data.provider || 'deepseek'
});

const distillationId = createResult.data.id;
```

**检查结果**: ✅ 正确
- 使用 `localDistillationApi.create()` 保存到本地数据库
- IPC 通道: `distillation:local:create`
- 目标数据库: `geo_windows.distillations` 表

#### ✅ 步骤 3: 保存话题到本地数据库

```typescript
// L249-263: 保存话题
for (let i = 0; i < questions.length; i++) {
  const question = questions[i];
  
  const topicResult = await window.electron.invoke('topic:local:create', {
    distillation_id: distillationId,
    question: question.question || question,
    category: question.category || '',
    priority: question.priority || 0
  });
  
  if (!topicResult.success) {
    throw new Error(`保存话题失败: ${topicResult.error}`);
  }
}
```

**检查结果**: ✅ 正确
- 使用 IPC 通道 `topic:local:create` 保存话题
- 目标数据库: `geo_windows.topics` 表
- 关联字段: `distillation_id`

#### ✅ 步骤 4: 读取蒸馏历史

```typescript
// L30-36: 读取历史记录
const fetchHistory = useCallback(async () => {
  const result = await localDistillationApi.findAll({ page: 1, pageSize: 1000 });
  if (!result.success) {
    throw new Error(result.error || '加载历史记录失败');
  }
  return result.data?.items || [];
}, []);
```

**检查结果**: ✅ 正确
- 使用 `localDistillationApi.findAll()` 从本地数据库读取
- IPC 通道: `distillation:local:findAll`
- 数据源: `geo_windows.distillations` 表

---

### 3.2 蒸馏结果页面（DistillationResultsPage.tsx）

#### ✅ 步骤 1: 读取蒸馏结果列表

```typescript
// L62-68: 数据获取函数
const fetchData = useCallback(async () => {
  const result = await fetchLocalResultsWithReferences({
    keyword: filterKeyword || undefined,
    search: searchText || undefined,
    page: currentPage,
    pageSize
  });
  return result;
}, [filterKeyword, searchText, currentPage, pageSize]);
```

**检查结果**: ✅ 正确
- 使用 `fetchLocalResultsWithReferences()` 从本地数据库读取
- IPC 通道: `distillation:local:getResults`
- 数据源: `geo_windows.topics` 表（JOIN `distillations` 表）

#### ✅ 步骤 2: 读取关键词列表

```typescript
// L72-78: 关键词列表缓存
const { 
  data: keywordsData,
  refresh: refreshKeywords 
} = useCachedData(
  'distillationResults:keywords',
  fetchLocalKeywords,
  { onError: () => console.error('加载关键词列表失败') }
);
```

**检查结果**: ✅ 正确
- 使用 `fetchLocalKeywords()` 从本地数据库读取
- IPC 通道: `distillation:local:getKeywords`
- 数据源: `geo_windows.topics` 表（DISTINCT keyword）

---

### 3.3 IPC 处理器（localDistillationHandlers.ts）

#### ✅ 蒸馏结果查询

```typescript
// L217-280: 获取蒸馏结果列表
ipcMain.handle('distillation:local:getResults', async (_event, filters?: any) => {
  // 构建查询条件
  const conditions: string[] = ['t.user_id = $1'];
  const params: any[] = [user.id];
  
  // 查询数据（包含引用次数）
  const dataQuery = `
    SELECT 
      t.id,
      t.question,
      t.distillation_id,
      t.created_at as "createdAt",
      d.keyword,
      COALESCE(
        (SELECT COUNT(*) FROM articles a WHERE a.topic_id = t.id),
        0
      ) as "referenceCount"
    FROM topics t
    JOIN distillations d ON t.distillation_id = d.id
    ${whereClause}
    ORDER BY t.created_at DESC
    LIMIT ${paramIndex} OFFSET ${paramIndex + 1}
  `;
  
  return { success: true, data: { data: dataResult.rows, total, ... } };
});
```

**检查结果**: ✅ 正确
- 从本地 PostgreSQL 数据库查询
- 数据库: `geo_windows`
- 表: `topics` JOIN `distillations`
- 包含引用次数统计

#### ✅ 关键词列表查询

```typescript
// L283-305: 获取所有唯一关键词列表
ipcMain.handle('distillation:local:getKeywords', async () => {
  const query = `
    SELECT DISTINCT d.keyword
    FROM distillations d
    INNER JOIN topics t ON d.id = t.distillation_id
    WHERE d.user_id = $1
    ORDER BY d.keyword ASC
  `;
  const result = await pool.query(query, [user.id]);
  
  return {
    success: true,
    data: { keywords: result.rows.map((row: any) => row.keyword) }
  };
});
```

**检查结果**: ✅ 正确
- 从本地 PostgreSQL 数据库查询
- 数据库: `geo_windows`
- 只返回有话题的关键词

---

## 四、服务器端 API 检查

### ✅ 蒸馏 API（server/src/routes/distillation.ts）

```typescript
// L15-78: 执行关键词蒸馏
distillationRouter.post('/', async (req, res) => {
  const userId = getCurrentTenantId(req);
  const { keyword } = req.body;
  
  // 1. 配额检查
  const quota = await usageTrackingService.checkQuota(userId, 'keyword_distillation');
  
  // 2. 使用 AI 服务生成话题
  const aiService = await ConfigHelper.getAIService();
  const questions = await aiService.distillKeyword(keyword, promptTemplate, topicCount);
  
  // 3. 保存蒸馏记录到服务器数据库（仅用于配额统计）
  const distillationResult = await pool.query(
    'INSERT INTO distillations (keyword, provider, user_id, created_at, updated_at) VALUES ($1, $2, $3, NOW(), NOW()) RETURNING id',
    [keyword, currentConfig.provider, userId]
  );
  
  // 4. 保存话题到服务器数据库（仅用于配额统计）
  for (const question of questions) {
    await pool.query(
      'INSERT INTO topics (distillation_id, question) VALUES ($1, $2)',
      [distillationId, question]
    );
  }
  
  // 5. 记录配额使用
  await usageTrackingService.recordUsage(userId, 'keyword_distillation', 'distillation', distillationId, 1);
  
  // 6. 返回话题列表给 Windows 端
  res.json({ success: true, distillationId, keyword, questions, count: questions.length });
});
```

**检查结果**: ✅ 正确
- 服务器端保存到 `geo_system` 数据库（用于配额统计）
- 返回话题列表给 Windows 端
- Windows 端再保存到本地 `geo_windows` 数据库

---

## 五、数据流验证

### ✅ 完整流程验证

1. **用户输入关键词** → ✅ 正确
   - 位置: `DistillationPage.tsx`
   - 输入框绑定: `keyword` 状态

2. **调用服务器 API** → ✅ 正确
   - API: `POST /api/distillation`
   - 参数: `{ keyword: keyword.trim() }`
   - 服务器数据库: `geo_system`（仅用于配额统计）

3. **AI 生成话题** → ✅ 正确
   - 服务: `AIService.distillKeyword()`
   - 提供商: DeepSeek/Gemini/Ollama
   - 返回: `questions` 数组

4. **保存到 Windows 端数据库** → ✅ 正确
   - 数据库: `geo_windows`
   - 表: `distillations` + `topics`
   - IPC 通道: `distillation:local:create` + `topic:local:create`

5. **显示蒸馏历史** → ✅ 正确
   - 数据源: `geo_windows.distillations`
   - IPC 通道: `distillation:local:findAll`

6. **显示蒸馏结果** → ✅ 正确
   - 数据源: `geo_windows.topics` JOIN `distillations`
   - IPC 通道: `distillation:local:getResults`

---

## 六、潜在问题检查

### ⚠️ 问题 1: 服务器端也保存了蒸馏记录

**现象**:
- 服务器端在 `geo_system` 数据库也保存了 `distillations` 和 `topics` 记录
- Windows 端在 `geo_windows` 数据库也保存了相同的记录

**分析**:
- 服务器端保存是为了配额统计和使用追踪
- Windows 端保存是为了本地查询和离线使用
- 两个数据库的记录 **不同步**，各自独立

**影响**:
- ✅ 不影响功能正常使用
- ✅ 符合设计意图（服务器端用于配额，Windows 端用于本地查询）

**建议**:
- 保持现状，无需修改
- 服务器端的记录用于配额管理
- Windows 端的记录用于用户界面显示

---

### ⚠️ 问题 2: 服务器端 `distillation_id` 与 Windows 端不一致

**现象**:
- 服务器端生成的 `distillationId` 是服务器数据库的 ID
- Windows 端保存时生成新的本地 ID
- 两个 ID 不一致

**分析**:
- 这是正常的，因为两个数据库独立
- Windows 端不需要使用服务器端的 ID

**影响**:
- ✅ 不影响功能
- ✅ 符合设计（两个数据库独立）

**建议**:
- 保持现状，无需修改

---

## 七、返回结果检查

### ✅ 服务器端返回结果

```typescript
// server/src/routes/distillation.ts L68-75
res.json({
  success: true,
  distillationId,      // 服务器端的 ID（用于配额统计）
  keyword,             // 关键词
  questions,           // 话题列表 ✅
  count: questions.length  // 话题数量 ✅
});
```

**检查结果**: ✅ 正确
- 返回了完整的话题列表
- Windows 端可以正确接收

### ✅ Windows 端接收结果

```typescript
// DistillationPage.tsx L227-229
const response = await apiClient.post('/distillation', { keyword: keyword.trim() });
const questions = response.data.questions || [];  // ✅ 正确接收
const count = questions.length;
```

**检查结果**: ✅ 正确
- 正确接收 `questions` 数组
- 正确计算 `count`

### ✅ Windows 端保存结果

```typescript
// DistillationPage.tsx L249-263
for (let i = 0; i < questions.length; i++) {
  const question = questions[i];
  
  const topicResult = await window.electron.invoke('topic:local:create', {
    distillation_id: distillationId,  // 本地 distillation ID
    question: question.question || question,  // ✅ 正确提取问题
    category: question.category || '',
    priority: question.priority || 0
  });
}
```

**检查结果**: ✅ 正确
- 正确遍历 `questions` 数组
- 正确保存到本地数据库

---

## 八、总结

### ✅ 所有检查项通过

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 数据库使用 | ✅ 正确 | 服务器端和 Windows 端各自使用正确的数据库 |
| API 调用 | ✅ 正确 | 正确调用服务器 API 生成话题 |
| 结果返回 | ✅ 正确 | 服务器正确返回话题列表 |
| 本地保存 | ✅ 正确 | Windows 端正确保存到本地数据库 |
| 历史显示 | ✅ 正确 | 从本地数据库正确读取历史记录 |
| 结果显示 | ✅ 正确 | 从本地数据库正确读取蒸馏结果 |

### 📋 数据流总结

```
用户输入关键词
    ↓
调用服务器 API（geo_system）
    ↓
AI 生成话题
    ↓
服务器返回话题列表 ✅
    ↓
保存到 Windows 端数据库（geo_windows）✅
    ↓
显示蒸馏历史（从 geo_windows 读取）✅
    ↓
显示蒸馏结果（从 geo_windows 读取）✅
```

### 🎯 结论

**所有数据流和数据库使用都是正确的！**

1. ✅ 服务器端正确使用 `geo_system` 数据库（用于配额统计）
2. ✅ Windows 端正确使用 `geo_windows` 数据库（用于本地查询）
3. ✅ 服务器端正确返回话题列表
4. ✅ Windows 端正确接收并保存话题
5. ✅ 蒸馏历史从本地数据库正确读取
6. ✅ 蒸馏结果从本地数据库正确读取

**无需修改任何代码！**

---

## 九、测试建议

### 测试步骤

1. **测试蒸馏功能**
   ```
   1. 打开关键词蒸馏页面
   2. 输入关键词（如"英国留学"）
   3. 点击"开始蒸馏"
   4. 等待服务器返回结果
   5. 检查是否自动跳转到蒸馏结果页面
   ```

2. **测试历史记录**
   ```
   1. 返回关键词蒸馏页面
   2. 检查"蒸馏历史"表格是否显示刚才的记录
   3. 点击"查看详情"按钮
   4. 检查是否正确显示话题列表
   ```

3. **测试蒸馏结果页面**
   ```
   1. 打开蒸馏结果页面
   2. 检查是否显示所有话题
   3. 使用关键词筛选功能
   4. 使用搜索功能
   5. 检查引用次数是否正确显示
   ```

### 预期结果

- ✅ 蒸馏成功生成话题
- ✅ 历史记录正确显示
- ✅ 蒸馏结果正确显示
- ✅ 筛选和搜索功能正常
- ✅ 引用次数统计正确

---

**检查完成时间**: 2026-01-18  
**检查结论**: ✅ 所有数据流和数据库使用都正确，无需修改
