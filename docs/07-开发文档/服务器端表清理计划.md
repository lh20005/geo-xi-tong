# 服务器端表清理计划

**创建日期**: 2026-01-16  
**状态**: 📋 待执行

---

## 背景

Windows 端已从 SQLite 迁移到 PostgreSQL，并从服务器端迁移了 17 个表到本地。现在需要确定：
1. 服务器端哪些表可以删除？
2. 服务器端哪些功能需要保留？

---

## 迁移到 Windows 端的表（17 个）

以下表已迁移到 Windows 端本地数据库（`geo_windows`）：

| 表名 | 用途 | 迁移状态 |
|------|------|---------|
| `articles` | 文章存储 | ✅ 已迁移 |
| `albums` | 图库相册 | ✅ 已迁移 |
| `images` | 图片管理 | ✅ 已迁移 |
| `knowledge_bases` | 知识库 | ✅ 已迁移 |
| `knowledge_documents` | 知识库文档 | ✅ 已迁移 |
| `platform_accounts` | 平台账号 | ✅ 已迁移 |
| `publishing_tasks` | 发布任务 | ✅ 已迁移 |
| `publishing_records` | 发布记录 | ✅ 已迁移 |
| `publishing_logs` | 发布日志 | ✅ 已迁移 |
| `conversion_targets` | 转化目标 | ✅ 已迁移 |
| `distillations` | 蒸馏任务 | ✅ 已迁移 |
| `topics` | 话题 | ✅ 已迁移 |
| `article_settings` | 文章设置 | ✅ 已迁移 |
| `distillation_config` | 蒸馏配置 | ✅ 已迁移 |
| `image_usage` | 图片使用记录 | ✅ 已迁移 |
| `distillation_usage` | 蒸馏使用记录 | ✅ 已迁移 |
| `topic_usage` | 话题使用记录 | ✅ 已迁移 |

---

## 服务器端表使用情况分析

### ❌ 需要删除的表和功能

以下表和功能**应该删除**，因为已完全迁移到 Windows 端：

#### 1. 文章相关（articles）

**当前服务器端使用情况**：
- ✅ `DashboardService.ts` - 统计文章数量
- ✅ `QuotaService.ts` - 统计文章数量
- ✅ `PublishingService.ts` - 读取文章内容、更新发布状态
- ✅ `articleGenerationService.ts` - 保存生成的文章
- ✅ `topicSelectionService.ts` - 更新文章的 topic_id
- ✅ `routes/article.ts` - 完整的文章 CRUD API

**删除原因**：
- 文章现在完全在 Windows 端本地存储
- 服务器端不再需要存储文章内容
- AI 生成的文章应该直接返回给客户端，由客户端保存

**需要删除的代码**：
- ❌ `server/src/routes/article.ts` - 整个文件
- ❌ `server/src/services/articleGenerationService.ts` 中的文章保存逻辑
- ❌ `DashboardService.ts` 中的文章统计（改为从客户端上报）
- ❌ `QuotaService.ts` 中的文章统计

**需要保留的功能**：
- ✅ AI 生成文章内容（但不保存到数据库）
- ✅ 配额验证和扣减

#### 2. 平台账号相关（platform_accounts）

**当前服务器端使用情况**：
- ✅ `AccountService.ts` - 完整的账号 CRUD
- ✅ `routes/platformAccounts.ts` - 账号管理 API
- ✅ `routes/publishingTasks.ts` - 验证账号所有权

**删除原因**：
- 平台账号（包括 Cookie）现在完全在 Windows 端本地加密存储
- 服务器端不应该存储用户的平台登录凭证（安全考虑）
- 发布任务完全在 Windows 端本地执行

**需要删除的代码**：
- ❌ `server/src/services/AccountService.ts` - 整个文件
- ❌ `server/src/routes/platformAccounts.ts` - 整个文件
- ❌ `routes/publishingTasks.ts` 中的账号验证逻辑

#### 3. 发布相关（publishing_tasks, publishing_records, publishing_logs）

**当前服务器端使用情况**：
- ✅ `PublishingService.ts` - 发布任务管理
- ✅ `routes/publishingTasks.ts` - 发布任务 API
- ✅ `DashboardService.ts` - 发布统计

**删除原因**：
- 发布任务现在完全在 Windows 端本地执行
- 发布记录和日志在本地存储
- 服务器端只需要接收统计上报

**需要删除的代码**：
- ❌ `server/src/services/PublishingService.ts` - 整个文件
- ❌ `server/src/routes/publishingTasks.ts` - 整个文件
- ❌ `DashboardService.ts` 中的发布统计（改为从客户端上报）

**需要保留的功能**：
- ✅ 发布统计上报接口（新增）
- ✅ 配额验证和扣减

#### 4. 知识库相关（knowledge_bases, knowledge_documents）

**当前服务器端使用情况**：
- ✅ `KnowledgeBaseService.ts` - 知识库管理
- ✅ `routes/knowledgeBase.ts` - 知识库 API
- ✅ `articleGenerationService.ts` - 读取知识库内容用于生成

**删除原因**：
- 知识库文件现在完全在 Windows 端本地存储
- 服务器端不需要存储用户的知识库文件

**需要删除的代码**：
- ❌ `server/src/services/KnowledgeBaseService.ts` - 整个文件
- ❌ `server/src/routes/knowledgeBase.ts` - 整个文件

**需要保留的功能**：
- ✅ AI 生成时，客户端可以将知识库内容作为参数传递给服务器

#### 5. 图库相关（albums, images, image_usage）

**当前服务器端使用情况**：
- ✅ `ImageService.ts` - 图片管理
- ✅ `routes/images.ts` - 图片 API
- ✅ `articleGenerationService.ts` - 图片引用

**删除原因**：
- 图片现在完全在 Windows 端本地存储
- 服务器端不需要存储用户的图片文件

**需要删除的代码**：
- ❌ `server/src/services/ImageService.ts` - 整个文件
- ❌ `server/src/routes/images.ts` - 整个文件

#### 6. 蒸馏相关（distillations, topics, conversion_targets, article_settings）

**当前服务器端使用情况**：
- ✅ `DistillationService.ts` - 蒸馏任务管理
- ✅ `TopicService.ts` - 话题管理
- ✅ `routes/distillation.ts` - 蒸馏 API
- ✅ `routes/topics.ts` - 话题 API

**删除原因**：
- 蒸馏任务和话题现在完全在 Windows 端本地存储
- 服务器端只需要提供 AI 生成能力

**需要删除的代码**：
- ❌ `server/src/services/DistillationService.ts` - 整个文件
- ❌ `server/src/services/TopicService.ts` - 整个文件
- ❌ `server/src/routes/distillation.ts` - 整个文件
- ❌ `server/src/routes/topics.ts` - 整个文件

---

## ✅ 服务器端需要保留的功能

服务器端应该**只保留**以下核心功能：

### 1. 用户认证和授权
- ✅ 用户注册、登录、JWT 管理
- ✅ 用户信息管理
- ✅ 权限验证

**保留的表**：
- `users`
- `user_sessions`
- `refresh_tokens`

### 2. 配额管理
- ✅ 配额验证
- ✅ 配额预扣减/确认/释放
- ✅ 配额统计

**保留的表**：
- `user_quotas`
- `quota_reservations`
- `quota_usage_logs`

### 3. 订阅系统
- ✅ 套餐管理
- ✅ 订单管理
- ✅ 支付集成（微信支付）
- ✅ 订阅状态管理

**保留的表**：
- `subscription_plans`
- `user_subscriptions`
- `orders`
- `payments`

### 4. AI 生成服务
- ✅ DeepSeek/Gemini API 调用
- ✅ 文章生成（返回内容，不保存）
- ✅ 话题生成（返回内容，不保存）
- ✅ 蒸馏生成（返回内容，不保存）

**保留的表**：
- `generation_tasks`（用于追踪生成任务状态）
- `ai_generation_cache`（可选，用于缓存）

### 5. 数据同步
- ✅ 数据快照上传/下载
- ✅ 快照管理（最多 3 个）

**保留的表**：
- `sync_snapshots`

### 6. 分析上报
- ✅ 发布统计上报
- ✅ 错误追踪
- ✅ 使用分析

**保留的表**：
- `analytics_events`
- `error_logs`

### 7. 代理商系统
- ✅ 代理商管理
- ✅ 邀请码管理
- ✅ 佣金管理

**保留的表**：
- `agents`
- `invitation_codes`
- `commissions`

### 8. 安全审计
- ✅ 安全日志
- ✅ 审计记录

**保留的表**：
- `security_logs`
- `audit_logs`

---

## 清理步骤

### 阶段 1：备份数据（强制）

```bash
# 1. 备份整个数据库
sudo -u postgres pg_dump -d geo_system -f backup_before_cleanup_$(date +%Y%m%d).sql

# 2. 备份要删除的表数据
sudo -u postgres pg_dump -d geo_system -t articles -t platform_accounts -t publishing_tasks -t publishing_records -t publishing_logs -t knowledge_bases -t knowledge_documents -t albums -t images -t distillations -t topics -t conversion_targets -t article_settings -t distillation_config -t image_usage -t distillation_usage -t topic_usage -f backup_deleted_tables_$(date +%Y%m%d).sql
```

### 阶段 2：删除服务器端代码

**删除的文件**：
```bash
# 服务层
rm server/src/services/AccountService.ts
rm server/src/services/PublishingService.ts
rm server/src/services/KnowledgeBaseService.ts
rm server/src/services/ImageService.ts
rm server/src/services/DistillationService.ts
rm server/src/services/TopicService.ts

# 路由层
rm server/src/routes/article.ts
rm server/src/routes/platformAccounts.ts
rm server/src/routes/publishingTasks.ts
rm server/src/routes/knowledgeBase.ts
rm server/src/routes/images.ts
rm server/src/routes/distillation.ts
rm server/src/routes/topics.ts
```

**修改的文件**：
```typescript
// server/src/services/articleGenerationService.ts
// 修改：生成文章后不保存到数据库，直接返回内容

// server/src/services/DashboardService.ts
// 修改：删除文章统计、发布统计（改为从客户端上报）

// server/src/services/QuotaService.ts
// 修改：删除文章数量统计
```

### 阶段 3：删除数据库表

```sql
-- 删除表（按依赖顺序）
DROP TABLE IF EXISTS image_usage CASCADE;
DROP TABLE IF EXISTS distillation_usage CASCADE;
DROP TABLE IF EXISTS topic_usage CASCADE;
DROP TABLE IF EXISTS publishing_logs CASCADE;
DROP TABLE IF EXISTS publishing_records CASCADE;
DROP TABLE IF EXISTS publishing_tasks CASCADE;
DROP TABLE IF EXISTS articles CASCADE;
DROP TABLE IF EXISTS topics CASCADE;
DROP TABLE IF EXISTS distillations CASCADE;
DROP TABLE IF EXISTS conversion_targets CASCADE;
DROP TABLE IF EXISTS article_settings CASCADE;
DROP TABLE IF EXISTS distillation_config CASCADE;
DROP TABLE IF EXISTS knowledge_documents CASCADE;
DROP TABLE IF EXISTS knowledge_bases CASCADE;
DROP TABLE IF EXISTS images CASCADE;
DROP TABLE IF EXISTS albums CASCADE;
DROP TABLE IF EXISTS platform_accounts CASCADE;
```

### 阶段 4：新增功能

**新增的 API**：
```typescript
// 1. 发布统计上报
POST /api/analytics/publish-report
{
  taskId: string;
  platform: string;
  status: 'success' | 'failed';
  duration: number;
  errorCode?: string;
  errorMessage?: string;
}

// 2. 批量发布统计上报
POST /api/analytics/publish-report/batch
{
  reports: PublishReport[];
}

// 3. 文章生成（不保存）
POST /api/article-generation/generate
Response: {
  generationId: string;
  article: { title, content, ... };
  expiresAt: string;
}

// 4. 确认收到生成的文章
POST /api/article-generation/confirm
{
  generationId: string;
}
```

### 阶段 5：更新文档

- ✅ 更新 API 文档
- ✅ 更新架构图
- ✅ 更新部署文档
- ✅ 更新 Steering 文件

---

## 影响评估

### 优点

1. **简化服务器端**：
   - 减少 17 个表
   - 减少大量代码
   - 降低维护成本

2. **提高安全性**：
   - 敏感数据（平台账号、Cookie）不再存储在服务器
   - 减少数据泄露风险

3. **提高性能**：
   - 减少数据库查询
   - 减少网络传输
   - 提高响应速度

4. **降低成本**：
   - 减少服务器存储空间
   - 减少数据库负载
   - 减少带宽消耗

### 缺点

1. **失去集中管理**：
   - 无法在服务器端查看所有用户的文章
   - 无法在服务器端统计发布数据（需要客户端上报）

2. **数据分析困难**：
   - 需要依赖客户端上报数据
   - 可能存在数据不完整的情况

### 解决方案

1. **数据上报机制**：
   - 客户端定期上报统计数据
   - 支持离线队列，网络恢复后上报

2. **数据同步**：
   - 用户可以选择将数据同步到云端（快照）
   - 用于备份和多设备同步

---

## 时间表

| 阶段 | 任务 | 预计时间 | 状态 |
|------|------|---------|------|
| 1 | 备份数据 | 1 小时 | ⏳ 待执行 |
| 2 | 删除服务器端代码 | 4 小时 | ⏳ 待执行 |
| 3 | 删除数据库表 | 1 小时 | ⏳ 待执行 |
| 4 | 新增功能 | 8 小时 | ⏳ 待执行 |
| 5 | 测试验证 | 4 小时 | ⏳ 待执行 |
| 6 | 更新文档 | 2 小时 | ⏳ 待执行 |

**总计**: 约 20 小时

---

## 风险和注意事项

### ⚠️ 高风险

1. **数据丢失风险**：
   - 必须先备份数据
   - 确认备份可恢复

2. **功能中断风险**：
   - 删除代码前确认没有其他依赖
   - 分阶段执行，每阶段测试

### ⚠️ 中风险

1. **客户端兼容性**：
   - 旧版本客户端可能调用已删除的 API
   - 需要强制更新客户端

2. **数据迁移**：
   - 现有用户的数据需要迁移到本地
   - 提供数据导出工具

### 解决方案

1. **分阶段执行**：
   - 先删除代码，保留表（标记为 deprecated）
   - 观察一段时间，确认无影响
   - 再删除表

2. **提供回滚方案**：
   - 保留备份
   - 准备回滚脚本

3. **客户端强制更新**：
   - 发布新版本客户端
   - 旧版本提示更新

---

## 决策

**建议**：✅ 执行清理

**原因**：
1. 符合架构设计（客户端本地存储 + 服务器云端服务）
2. 提高安全性和性能
3. 降低维护成本
4. 简化服务器端代码

**前提条件**：
1. ✅ Windows 端 PostgreSQL 迁移已完成
2. ✅ Windows 端功能已测试通过
3. ⏳ 数据已备份
4. ⏳ 客户端已更新

---

## 总结

**需要删除的表**（17 个）：
- articles, albums, images
- knowledge_bases, knowledge_documents
- platform_accounts
- publishing_tasks, publishing_records, publishing_logs
- conversion_targets, distillations, topics
- article_settings, distillation_config
- image_usage, distillation_usage, topic_usage

**需要保留的功能**：
- 用户认证、配额管理、订阅系统
- AI 生成服务（不保存结果）
- 数据同步、分析上报
- 代理商系统、安全审计

**下一步**：
1. 确认是否执行清理
2. 备份数据
3. 分阶段执行清理
4. 测试验证
5. 更新文档
