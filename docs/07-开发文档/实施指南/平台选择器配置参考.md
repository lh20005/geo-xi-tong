# 平台选择器配置参考

## 📋 概述

本文档记录了各平台用户名提取的选择器配置，方便后续调整和优化。

## 🎯 选择器配置原则

1. **优先级从高到低**
   - 特定选择器（平台专用）
   - 通用选择器（常见class名）
   - 备用选择器（更通用）
   - 通配符选择器（匹配动态class）

2. **选择器数量**
   - 每个平台配置4-6个选择器
   - 确保有足够的备选方案

3. **命名规范**
   - 优先使用语义化class名
   - 避免使用过于通用的选择器
   - 考虑动态class名的情况

## 🔧 各平台选择器配置

### 自媒体平台

#### 头条号 (toutiao) ✅ 已测试成功

```typescript
'toutiao': [
  '.auth-avator-name',              // 优先级1：头条号特定选择器
  '.semi-navigation-header-username', // 优先级2：Semi UI组件
  '.user-name',                     // 优先级3：通用选择器
  '.username',                      // 优先级4：备用选择器
  '.account-name',                  // 优先级5：账号名称
  '[class*="username"]',            // 优先级6：通配符
  '[class*="user-name"]'            // 优先级7：通配符备用
]
```

**登录URL:** `https://mp.toutiao.com/auth/page/login/`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ✅ 成功

---

#### 网易号 (wangyi)

```typescript
'wangyi': [
  '.user-info .name',               // 优先级1：网易号特定选择器
  '.user-name',                     // 优先级2：通用选择器
  '.username',                      // 优先级3：备用选择器
  '.account-name',                  // 优先级4：账号名称
  '[class*="user-name"]',           // 优先级5：通配符
  '[class*="username"]'             // 优先级6：通配符备用
]
```

**登录URL:** `https://mp.163.com/login.html`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### 搜狐号 (souhu)

```typescript
'souhu': [
  '.user-name',                     // 优先级1：搜狐号选择器
  '.username',                      // 优先级2：通用选择器
  '.account-name',                  // 优先级3：账号名称
  '.author-name',                   // 优先级4：作者名称
  '[class*="user-name"]',           // 优先级5：通配符
  '[class*="username"]'             // 优先级6：通配符备用
]
```

**登录URL:** `https://mp.sohu.com/login`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### 百家号 (baijiahao)

```typescript
'baijiahao': [
  '.author-name',                   // 优先级1：百家号特定选择器
  '.user-name',                     // 优先级2：通用选择器
  '.username',                      // 优先级3：备用选择器
  '.account-name',                  // 优先级4：账号名称
  '[class*="author-name"]',         // 优先级5：通配符
  '[class*="user-name"]'            // 优先级6：通配符备用
]
```

**登录URL:** `https://baijiahao.baidu.com/builder/author/register/index`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### 企鹅号 (qie)

```typescript
'qie': [
  '.user-info-name',                // 优先级1：企鹅号特定选择器
  '.user-name',                     // 优先级2：通用选择器
  '.username',                      // 优先级3：备用选择器
  '.account-name',                  // 优先级4：账号名称
  '[class*="user-name"]',           // 优先级5：通配符
  '[class*="username"]'             // 优先级6：通配符备用
]
```

**登录URL:** `https://om.qq.com/userAuth/index`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

### 社交媒体平台

#### 小红书 (xiaohongshu)

```typescript
'xiaohongshu': [
  '.username',                      // 优先级1：小红书选择器
  '.user-name',                     // 优先级2：通用选择器
  '.nickname',                      // 优先级3：昵称
  '.author-name',                   // 优先级4：作者名称
  '[class*="username"]',            // 优先级5：通配符
  '[class*="nickname"]'             // 优先级6：通配符备用
]
```

**登录URL:** `https://creator.xiaohongshu.com/login`  
**登录检测:** 元素检测（混合）  
**需要导航:** 是 → `https://creator.xiaohongshu.com/creator/home` (等待3秒)  
**测试状态:** ⏳ 待测试

---

#### 微信公众号 (wechat)

```typescript
'wechat': [
  '.account_info_title',            // 优先级1：微信特定选择器
  '.account-name',                  // 优先级2：账号名称
  '.user-name',                     // 优先级3：通用选择器
  '.username',                      // 优先级4：备用选择器
  '[class*="account"]',             // 优先级5：通配符
  '[class*="user-name"]'            // 优先级6：通配符备用
]
```

**登录URL:** `https://mp.weixin.qq.com/`  
**登录检测:** 元素检测（混合）  
**需要导航:** 是 → `https://mp.weixin.qq.com/` (等待3秒)  
**测试状态:** ⏳ 待测试

---

#### B站 (bilibili)

```typescript
'bilibili': [
  '.user-name',                     // 优先级1：B站选择器
  '.username',                      // 优先级2：通用选择器
  '.uname',                         // 优先级3：B站特定
  '.up-name',                       // 优先级4：UP主名称
  '[class*="user-name"]',           // 优先级5：通配符
  '[class*="username"]'             // 优先级6：通配符备用
]
```

**登录URL:** `https://member.bilibili.com/platform/home`  
**登录检测:** 元素检测（混合）  
**需要导航:** 是 → `https://member.bilibili.com/platform/home` (等待3秒)  
**测试状态:** ⏳ 待测试

---

### 技术社区平台

#### 知乎 (zhihu)

```typescript
'zhihu': [
  '.AppHeader-profile',             // 优先级1：知乎特定选择器
  '.Profile-name',                  // 优先级2：个人资料名称
  '.username',                      // 优先级3：通用选择器
  '.user-name',                     // 优先级4：备用选择器
  '[class*="Profile"]',             // 优先级5：通配符
  '[class*="username"]'             // 优先级6：通配符备用
]
```

**登录URL:** `https://www.zhihu.com/signin`  
**登录检测:** 元素检测（混合）  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### 简书 (jianshu)

```typescript
'jianshu': [
  '.user-name',                     // 优先级1：简书选择器
  '.username',                      // 优先级2：通用选择器
  '.nickname',                      // 优先级3：昵称
  '.author-name',                   // 优先级4：作者名称
  '[class*="user-name"]',           // 优先级5：通配符
  '[class*="nickname"]'             // 优先级6：通配符备用
]
```

**登录URL:** `https://www.jianshu.com/sign_in`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### CSDN (csdn)

```typescript
'csdn': [
  '.user-name',                     // 优先级1：CSDN选择器
  '.username',                      // 优先级2：通用选择器
  '.nick-name',                     // 优先级3：昵称
  '.user-profile-name',             // 优先级4：个人资料名称
  '[class*="user-name"]',           // 优先级5：通配符
  '[class*="username"]'             // 优先级6：通配符备用
]
```

**登录URL:** `https://passport.csdn.net/login`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### 掘金 (juejin)

```typescript
'juejin': [
  '.username',                      // 优先级1：掘金选择器
  '.user-name',                     // 优先级2：通用选择器
  '.author-name',                   // 优先级3：作者名称
  '[class*="username"]',            // 优先级4：通配符
  '[class*="user-name"]'            // 优先级5：通配符备用
]
```

**登录URL:** `https://juejin.cn/login`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### SegmentFault (segmentfault)

```typescript
'segmentfault': [
  '.user-name',                     // 优先级1：SF选择器
  '.username',                      // 优先级2：通用选择器
  '.author-name',                   // 优先级3：作者名称
  '[class*="user-name"]',           // 优先级4：通配符
  '[class*="username"]'             // 优先级5：通配符备用
]
```

**登录URL:** `https://segmentfault.com/user/login`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### 开源中国 (oschina)

```typescript
'oschina': [
  '.user-name',                     // 优先级1：开源中国选择器
  '.username',                      // 优先级2：通用选择器
  '.author-name',                   // 优先级3：作者名称
  '[class*="user-name"]',           // 优先级4：通配符
  '[class*="username"]'             // 优先级5：通配符备用
]
```

**登录URL:** `https://www.oschina.net/home/login`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### 博客园 (cnblogs)

```typescript
'cnblogs': [
  '.user-name',                     // 优先级1：博客园选择器
  '.username',                      // 优先级2：通用选择器
  '.author-name',                   // 优先级3：作者名称
  '[class*="user-name"]',           // 优先级4：通配符
  '[class*="username"]'             // 优先级5：通配符备用
]
```

**登录URL:** `https://account.cnblogs.com/signin`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

#### V2EX (v2ex)

```typescript
'v2ex': [
  '.username',                      // 优先级1：V2EX选择器
  '.user-name',                     // 优先级2：通用选择器
  '[class*="username"]',            // 优先级3：通配符
  '[class*="user-name"]'            // 优先级4：通配符备用
]
```

**登录URL:** `https://www.v2ex.com/signin`  
**登录检测:** URL变化  
**需要导航:** 否  
**测试状态:** ⏳ 待测试

---

## 🔧 如何更新选择器

### 1. 测试发现问题

运行测试脚本：
```bash
./test-platform-login.sh wangyi
```

如果用户名提取失败，会看到：
```
⚠️  所有选择器都未能提取到用户名
📄 已保存页面HTML: debug/wangyi_1234567890.html
```

### 2. 分析HTML快照

打开HTML文件：
```bash
open debug/wangyi_1234567890.html
```

搜索用户名，找到对应的HTML元素：
```html
<div class="user-info">
  <span class="name">测试用户</span>
</div>
```

### 3. 更新选择器配置

编辑 `server/src/services/AccountService.ts`：

```typescript
const selectors: { [key: string]: string[] } = {
  'wangyi': [
    '.user-info .name',      // ✅ 从HTML中找到的正确选择器
    '.user-name',
    '.username',
    '.account-name',
    '[class*="user-name"]',
    '[class*="username"]'
  ]
};
```

### 4. 重新测试

```bash
# 重启服务
./重启GEO系统.command

# 重新测试
./test-platform-login.sh wangyi
```

## 📊 选择器优先级说明

### 优先级1：平台特定选择器
- 最可靠，专门为该平台设计
- 例如：`.auth-avator-name`（头条号）
- 例如：`.account_info_title`（微信公众号）

### 优先级2-3：通用选择器
- 常见的class名
- 例如：`.user-name`, `.username`
- 适用于大多数平台

### 优先级4-5：备用选择器
- 更通用的选择器
- 例如：`.account-name`, `.author-name`
- 作为备选方案

### 优先级6-7：通配符选择器
- 匹配动态class名
- 例如：`[class*="username"]`
- 应对class名包含随机字符的情况

## 🎯 最佳实践

### 1. 选择器命名
- ✅ 使用语义化的class名
- ✅ 优先使用特定选择器
- ❌ 避免使用过于通用的选择器（如 `.name`）

### 2. 选择器数量
- ✅ 每个平台4-6个选择器
- ✅ 包含通配符选择器
- ❌ 不要配置过多选择器（影响性能）

### 3. 测试验证
- ✅ 每次更新后重新测试
- ✅ 保存HTML快照用于调试
- ✅ 记录测试结果

### 4. 文档更新
- ✅ 更新本文档
- ✅ 标记测试状态
- ✅ 记录特殊情况

## 📝 测试记录模板

```markdown
### 平台名称 (platform_id)

**测试日期:** 2024-01-01  
**测试人员:** XXX  
**测试结果:** ✅ 成功 / ❌ 失败  

**问题描述:**
- 用户名提取失败
- 选择器 `.user-name` 不存在

**解决方案:**
- 更新选择器为 `.user-info .name`
- 添加通配符选择器 `[class*="user"]`

**最终配置:**
```typescript
'platform_id': [
  '.user-info .name',
  '.user-name',
  '[class*="user"]'
]
```

**验证结果:** ✅ 成功提取用户名
```

## 🚀 下一步

1. **逐个测试平台**
   - 按照使用指南的顺序
   - 记录测试结果
   - 更新本文档

2. **优化选择器**
   - 根据实际测试结果
   - 调整选择器优先级
   - 添加新的选择器

3. **完善文档**
   - 更新测试状态
   - 记录特殊情况
   - 分享最佳实践

## 📁 相关文件

- `server/src/services/AccountService.ts` - 选择器配置文件
- `debug/` - HTML快照目录
- `test-platform-login.sh` - 测试脚本
- `平台登录器使用指南.md` - 使用说明
- `✅平台登录器开发完成.md` - 开发文档
