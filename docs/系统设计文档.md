# GEO优化系统 - 完整设计文档

## 一、系统概述

### 1.1 项目背景
GEO（Generative Engine Optimization）优化系统是一个专业的品牌AI推荐优化工具，旨在帮助品牌提升在AI平台（如ChatGPT、Claude等）的主动推荐率。

### 1.2 核心价值
- **智能关键词分析**: 基于AI的关键词蒸馏，生成真实用户搜索问题
- **精准话题管理**: 系统化管理和优化蒸馏后的话题
- **高质量内容生成**: 自动生成符合SEO标准的专业文章
- **多模型支持**: 灵活切换DeepSeek和Gemini AI模型

## 二、技术架构

### 2.1 整体架构
```
┌─────────────────────────────────────────────────────────┐
│                      前端层 (React)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ 工作台   │  │ 关键词   │  │ 话题管理 │  │ 文章生成│ │
│  │ Dashboard│  │ 蒸馏     │  │ Topics   │  │ Article │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
└─────────────────────────────────────────────────────────┘
                          ↕ HTTP/REST API
┌─────────────────────────────────────────────────────────┐
│                    后端层 (Node.js)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ API配置  │  │ 蒸馏服务 │  │ 话题服务 │  │ 文章服务│ │
│  │ Config   │  │ Distill  │  │ Topics   │  │ Article │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
│                    ┌──────────────┐                      │
│                    │  AI Service  │                      │
│                    └──────────────┘                      │
└─────────────────────────────────────────────────────────┘
                          ↕
┌─────────────────────────────────────────────────────────┐
│                   数据层 (PostgreSQL)                    │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌─────────┐ │
│  │ API配置表│  │ 蒸馏记录 │  │ 话题表   │  │ 文章表  │ │
│  └──────────┘  └──────────┘  └──────────┘  └─────────┘ │
└─────────────────────────────────────────────────────────┘
                          ↕
┌─────────────────────────────────────────────────────────┐
│                   AI服务层 (External)                    │
│         ┌──────────────┐      ┌──────────────┐          │
│         │  DeepSeek    │      │   Gemini     │          │
│         │     API      │      │     API      │          │
│         └──────────────┘      └──────────────┘          │
└─────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

#### 前端技术栈
- **框架**: React 18 + TypeScript
- **路由**: React Router v6
- **UI组件**: Ant Design 5
- **样式**: Tailwind CSS
- **状态管理**: Zustand (可选)
- **HTTP客户端**: Axios
- **构建工具**: Vite

#### 后端技术栈
- **运行时**: Node.js
- **框架**: Express
- **语言**: TypeScript
- **数据库**: PostgreSQL
- **ORM**: 原生 pg 驱动
- **AI集成**: DeepSeek API / Gemini API

## 三、数据库设计

### 3.1 数据表结构

> 📖 **完整数据库文档**: 查看 [数据库文档.md](./数据库文档.md) 获取详细的表结构、关系图和查询示例

系统包含 **12 个数据表**，分为 4 类：

#### 核心业务表 (4 个)

**api_configs (API配置表)**
```sql
CREATE TABLE api_configs (
  id SERIAL PRIMARY KEY,
  provider VARCHAR(20) NOT NULL,      -- 'deepseek' | 'gemini' | 'ollama'
  api_key TEXT,                       -- API密钥（Ollama可为空）
  ollama_base_url TEXT,               -- Ollama服务地址
  ollama_model TEXT,                  -- Ollama模型名称
  is_active BOOLEAN DEFAULT true,     -- 是否激活
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

**distillations (蒸馏记录表)**
```sql
CREATE TABLE distillations (
  id SERIAL PRIMARY KEY,
  keyword VARCHAR(255) NOT NULL,      -- 关键词
  provider VARCHAR(20) NOT NULL,      -- 使用的AI模型
  created_at TIMESTAMP DEFAULT NOW()
);
```

**topics (话题表)**
```sql
CREATE TABLE topics (
  id SERIAL PRIMARY KEY,
  distillation_id INTEGER REFERENCES distillations(id) ON DELETE CASCADE,
  question TEXT NOT NULL,             -- 蒸馏出的问题
  created_at TIMESTAMP DEFAULT NOW()
);
```

**articles (文章表)**
```sql
CREATE TABLE articles (
  id SERIAL PRIMARY KEY,
  title VARCHAR(500),                 -- 文章标题
  keyword VARCHAR(255) NOT NULL,      -- 关键词
  distillation_id INTEGER REFERENCES distillations(id),
  task_id INTEGER REFERENCES generation_tasks(id) ON DELETE SET NULL,
  requirements TEXT,                  -- 文章要求
  content TEXT NOT NULL,              -- 文章内容
  image_url TEXT,                     -- 配图URL
  provider VARCHAR(20) NOT NULL,      -- 使用的AI模型
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

#### 任务管理表 (3 个)

**generation_tasks (文章生成任务表)**
```sql
CREATE TABLE generation_tasks (
  id SERIAL PRIMARY KEY,
  status VARCHAR(20) DEFAULT 'pending',  -- 任务状态
  distillation_id INTEGER REFERENCES distillations(id),
  album_id INTEGER REFERENCES albums(id),
  knowledge_base_id INTEGER REFERENCES knowledge_bases(id),
  article_setting_id INTEGER REFERENCES article_settings(id),
  conversion_target_id INTEGER REFERENCES conversion_targets(id),
  article_count INTEGER NOT NULL,
  generated_count INTEGER DEFAULT 0,
  selected_distillations JSONB,       -- 选中的蒸馏结果
  error_message TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  completed_at TIMESTAMP
);
```

**article_settings (文章设置表)**
```sql
CREATE TABLE article_settings (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,         -- 设置名称
  word_count INTEGER,                 -- 字数要求
  style TEXT,                         -- 文章风格
  requirements TEXT,                  -- 其他要求
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

**conversion_targets (转化目标表)**
```sql
CREATE TABLE conversion_targets (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,         -- 目标名称
  type VARCHAR(50) NOT NULL,          -- 目标类型
  content TEXT NOT NULL,              -- 目标内容
  description TEXT,                   -- 描述
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

#### 资源管理表 (4 个)

**albums (相册表)**
```sql
CREATE TABLE albums (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,         -- 相册名称
  created_at TIMESTAMP DEFAULT NOW()
);
```

**images (图片表)**
```sql
CREATE TABLE images (
  id SERIAL PRIMARY KEY,
  album_id INTEGER REFERENCES albums(id) ON DELETE CASCADE,
  filename VARCHAR(255) NOT NULL,     -- 原始文件名
  filepath VARCHAR(500) NOT NULL,     -- 存储路径
  mime_type VARCHAR(100),             -- MIME类型
  size INTEGER,                       -- 文件大小
  created_at TIMESTAMP DEFAULT NOW()
);
```

**knowledge_bases (知识库表)**
```sql
CREATE TABLE knowledge_bases (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,         -- 知识库名称
  description TEXT,                   -- 描述
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

**knowledge_documents (知识文档表)**
```sql
CREATE TABLE knowledge_documents (
  id SERIAL PRIMARY KEY,
  knowledge_base_id INTEGER REFERENCES knowledge_bases(id) ON DELETE CASCADE,
  filename VARCHAR(255) NOT NULL,     -- 文件名
  file_type VARCHAR(50),              -- 文件类型
  file_size INTEGER,                  -- 文件大小
  content TEXT,                       -- 提取的文本内容
  created_at TIMESTAMP DEFAULT NOW()
);
```

#### 统计表 (1 个)

**distillation_usage (蒸馏使用统计表)**
```sql
CREATE TABLE distillation_usage (
  id SERIAL PRIMARY KEY,
  distillation_id INTEGER REFERENCES distillations(id) ON DELETE CASCADE,
  article_id INTEGER REFERENCES articles(id) ON DELETE CASCADE,
  used_at TIMESTAMP DEFAULT NOW()
);
```

### 3.2 数据关系

```
distillations (1) ──┬─→ (N) topics
                   ├─→ (N) articles
                   └─→ (N) distillation_usage

generation_tasks (1) ──→ (N) articles

albums (1) ──→ (N) images

knowledge_bases (1) ──→ (N) knowledge_documents

article_settings (1) ──→ (N) generation_tasks

conversion_targets (1) ──→ (N) generation_tasks
```

## 四、核心功能设计

### 4.1 API配置管理

**功能描述**: 用户可以配置和切换DeepSeek或Gemini的API密钥

**接口设计**:
- `GET /api/config/active` - 获取当前激活的配置
- `POST /api/config` - 保存新的API配置
- `POST /api/config/test` - 测试API连接

**业务流程**:
1. 用户选择AI模型（DeepSeek/Gemini）
2. 输入对应的API Key
3. 系统验证并保存配置
4. 停用其他配置，激活当前配置

### 4.2 关键词蒸馏

**功能描述**: 输入关键词，AI分析生成真实用户可能提出的相关问题

**接口设计**:
- `POST /api/distillation` - 执行关键词蒸馏
- `GET /api/distillation/history` - 获取蒸馏历史

**业务流程**:
1. 用户输入关键词（如"英国留学"）
2. 系统调用AI服务进行蒸馏分析
3. AI生成10-15个真实用户问题
4. 保存蒸馏记录和话题到数据库
5. 返回结果给前端展示

**AI提示词设计**:
```
你是一个专业的搜索行为分析专家。请根据关键词"{keyword}"，
生成10-15个真实用户在互联网搜索时可能提出的问题。

要求：
1. 问题要符合真实用户的搜索习惯
2. 包含不同的搜索意图（比较、推荐、评价等）
3. 使用常见的搜索词组合，如"哪家好"、"靠谱的"、"口碑好的"等
4. 问题要自然、口语化
```

### 4.3 话题管理

**功能描述**: 查看、编辑、删除蒸馏后的话题

**接口设计**:
- `GET /api/topics/:distillationId` - 获取指定蒸馏的所有话题
- `PUT /api/topics/:id` - 编辑话题
- `DELETE /api/topics/:id` - 删除话题

**业务流程**:
1. 展示蒸馏后的所有话题
2. 用户可以编辑话题内容
3. 用户可以删除不需要的话题
4. 用户可以选择话题用于文章生成

### 4.4 文章生成

**功能描述**: 基于关键词和话题，AI生成高质量SEO文章，支持引用知识库

**接口设计**:
- `POST /api/articles/generate` - 生成文章
- `GET /api/articles` - 获取文章列表
- `GET /api/articles/:id` - 获取文章详情
- `PUT /api/articles/:id` - 更新文章
- `DELETE /api/articles/:id` - 删除文章

**业务流程**:
1. 用户选择要使用的话题（可选）
2. 选择要引用的知识库（可选）
3. 输入文章生成要求
4. 系统调用AI生成文章
5. 保存文章到数据库
6. 展示生成的文章
7. 提供复制和下载功能

**AI提示词设计**:
```
你是一个专业的内容创作专家，擅长撰写高质量的SEO优化文章。

核心关键词：{keyword}

相关话题：
{topics列表}

{如果有知识库}
企业知识库参考：
{知识库文档内容}
{/如果有知识库}

文章要求：
{用户输入的要求}

请撰写一篇专业、高质量的文章，要求：
1. 文章要围绕核心关键词展开
2. 自然融入相关话题中的问题和答案
3. 如果提供了知识库，请基于知识库内容确保信息准确
4. 内容要有价值、有深度，对读者有实际帮助
5. 语言流畅、专业，符合SEO优化标准
6. 结构清晰，包含标题、段落、小标题等
```

### 4.5 文章生成任务

**功能描述**: 批量生成文章，支持任务管理和监控

**接口设计**:
- `POST /api/article-generation/tasks` - 创建任务
- `GET /api/article-generation/tasks` - 获取任务列表
- `GET /api/article-generation/tasks/:id` - 获取任务详情
- `GET /api/article-generation/tasks/:id/diagnose` - 诊断任务
- `POST /api/article-generation/tasks/:id/retry` - 重试任务
- `DELETE /api/article-generation/tasks/:id` - 删除任务
- `POST /api/article-generation/tasks/batch-delete` - 批量删除任务
- `DELETE /api/article-generation/tasks` - 删除所有任务

**业务流程**:
1. 用户创建生成任务，指定：
   - 蒸馏历史（关键词来源）
   - 图库（配图来源）
   - 知识库（内容参考）
   - 文章设置（模板）
   - 转化目标（可选）
   - 文章数量
2. 系统智能选择蒸馏结果
3. 后台异步生成文章
4. 用户可以监控任务状态
5. 任务完成后查看生成的文章
6. 支持任务诊断和重试

**任务状态**:
- `pending`: 等待执行
- `running`: 正在执行
- `completed`: 已完成
- `failed`: 执行失败

### 4.6 文章设置管理

**功能描述**: 管理文章生成的模板设置

**接口设计**:
- `GET /api/article-settings` - 获取设置列表
- `POST /api/article-settings` - 创建设置
- `GET /api/article-settings/:id` - 获取设置详情
- `PUT /api/article-settings/:id` - 更新设置
- `DELETE /api/article-settings/:id` - 删除设置

**业务流程**:
1. 创建文章设置模板
2. 配置字数、风格、要求
3. 在生成任务中使用模板
4. 支持模板复用和修改

**设置字段**:
- 名称：模板名称
- 字数：文章字数要求
- 风格：文章风格描述
- 要求：其他自定义要求

### 4.7 转化目标管理

**功能描述**: 管理文章中的转化目标（联系方式、产品链接等）

**接口设计**:
- `GET /api/conversion-targets` - 获取目标列表
- `POST /api/conversion-targets` - 创建目标
- `GET /api/conversion-targets/:id` - 获取目标详情
- `PUT /api/conversion-targets/:id` - 更新目标
- `DELETE /api/conversion-targets/:id` - 删除目标

**业务流程**:
1. 创建转化目标
2. 配置目标类型和内容
3. 在生成任务中关联目标
4. AI自动在文章中嵌入转化信息

**目标类型**:
- `phone`: 电话号码
- `email`: 电子邮件
- `url`: 网址链接
- `wechat`: 微信号
- `other`: 其他

### 4.8 企业图库管理

**功能描述**: 管理企业图片资源，用于文章配图

**接口设计**:
- `GET /api/gallery/albums` - 获取相册列表
- `POST /api/gallery/albums` - 创建相册
- `GET /api/gallery/albums/:id` - 获取相册详情
- `PATCH /api/gallery/albums/:id` - 更新相册
- `DELETE /api/gallery/albums/:id` - 删除相册
- `POST /api/gallery/albums/:albumId/images` - 上传图片
- `GET /api/gallery/images/:id` - 获取图片
- `DELETE /api/gallery/images/:id` - 删除图片

**业务流程**:
1. 创建相册分类
2. 批量上传图片
3. 在生成任务中选择图库
4. 系统自动为文章配图

**文件限制**:
- 支持格式: JPEG, PNG, GIF, WebP
- 单张图片最大: 5MB
- 批量上传最多: 20 张

### 4.9 企业知识库管理

**功能描述**: 管理企业知识文档，AI生成文章时引用

**接口设计**:
- `GET /api/knowledge-bases` - 获取知识库列表
- `POST /api/knowledge-bases` - 创建知识库
- `GET /api/knowledge-bases/:id` - 获取知识库详情
- `PATCH /api/knowledge-bases/:id` - 更新知识库
- `DELETE /api/knowledge-bases/:id` - 删除知识库
- `POST /api/knowledge-bases/:id/documents` - 上传文档
- `GET /api/knowledge-bases/documents/:id` - 获取文档详情
- `DELETE /api/knowledge-bases/documents/:id` - 删除文档
- `GET /api/knowledge-bases/:id/documents/search` - 搜索文档

**业务流程**:
1. 创建知识库分类
2. 上传企业文档
3. 系统自动提取文本内容
4. 在生成文章时引用知识库
5. AI基于知识库生成专业内容

**支持格式**:
- `.txt` - 纯文本
- `.md` - Markdown
- `.pdf` - PDF 文档
- `.doc` - Word 文档
- `.docx` - Word 文档

**文件限制**:
- 单个文件最大: 10MB
- 批量上传最多: 20 个文件

**核心价值**:
- 确保AI生成内容的准确性
- 融入企业专业知识
- 提升文章质量和可信度
- 减少人工审核工作

## 五、UI设计方案

### 5.1 设计风格

**整体风格**: 专业、现代、高端

**色彩方案**:
- 主色调: 天蓝色 (#0ea5e9) - 代表科技、智能
- 辅助色: 深灰色 (#1e293b) - 专业、稳重
- 背景色: 浅灰色 (#f5f7fa) - 简洁、清爽
- 强调色: 渐变色 - 增加视觉吸引力

**字体**:
- 中文: 系统默认字体
- 英文: -apple-system, BlinkMacSystemFont
- 字号: 14px (正文), 16-20px (标题)

### 5.2 页面布局

#### 整体布局
```
┌─────────────────────────────────────────────────┐
│  Sidebar  │           Header                    │
│           ├─────────────────────────────────────┤
│  ┌─────┐  │                                     │
│  │工作台│  │                                     │
│  ├─────┤  │                                     │
│  │蒸馏 │  │          Content Area               │
│  ├─────┤  │                                     │
│  │文章 │  │                                     │
│  ├─────┤  │                                     │
│  │配置 │  │                                     │
│  └─────┘  │                                     │
└─────────────────────────────────────────────────┘
```

#### 侧边栏 (Sidebar)
- 宽度: 240px
- 背景: 深色渐变 (#1e293b → #0f172a)
- Logo区域: 64px高度
- 菜单项: 图标 + 文字

#### 顶部栏 (Header)
- 高度: 64px
- 背景: 白色
- 显示: 页面标题 + API状态

#### 内容区 (Content)
- 背景: 白色卡片
- 圆角: 8px
- 阴影: 轻微阴影效果
- 内边距: 24px

### 5.3 页面设计

#### 1. 工作台 (Dashboard)
**布局**: 卡片式布局
- 顶部: 欢迎标题 + 描述
- 统计卡片: 4个渐变色卡片展示核心功能
- 快速开始: 引导用户配置和使用
- 系统特点: 展示系统优势

#### 2. API配置页面
**布局**: 居中表单
- 表单宽度: 最大800px
- 当前配置状态: 顶部Alert提示
- 配置表单: 
  - AI模型选择下拉框
  - API Key输入框（密码类型）
  - 保存按钮
- 帮助信息: 底部卡片展示获取API Key的链接

#### 3. 关键词蒸馏页面
**布局**: 上下结构
- 输入区域:
  - 标题 + 描述
  - 关键词输入框 + 蒸馏按钮
- 结果展示:
  - 卡片式展示蒸馏结果
  - 每个问题独立卡片，左侧蓝色边框
  - 操作按钮: 查看详情、生成文章
- 历史记录:
  - 表格展示历史蒸馏记录
  - 操作列: 查看话题、生成文章

#### 4. 话题管理页面
**布局**: 列表式
- 顶部: 返回按钮 + 关键词标签 + 生成文章按钮
- 话题列表:
  - 每个话题独立卡片
  - 左侧: 复选框（用于选择）
  - 中间: 话题内容
  - 右侧: 编辑、删除按钮
  - 选中状态: 蓝色边框高亮

#### 5. 文章生成页面
**布局**: 上下结构
- 顶部: 返回按钮 + 关键词显示
- 要求输入区:
  - 多行文本框
  - 生成按钮
- 加载状态: 居中Spin + 提示文字
- 结果展示:
  - 大卡片展示文章内容
  - 操作按钮: 复制、下载
  - 文章内容: 白色背景，边框，可滚动

#### 6. 文章管理页面
**布局**: 表格式
- 表格列: 关键词、预览、AI模型、创建时间、操作
- 操作: 查看、删除
- 查看弹窗: Modal展示完整文章内容

### 5.4 交互设计

#### 按钮状态
- 默认: 蓝色主题
- 悬停: 颜色加深
- 加载: 显示loading图标
- 禁用: 灰色，不可点击

#### 表单验证
- 实时验证
- 错误提示: 红色文字
- 成功提示: 绿色Toast

#### 加载状态
- 列表加载: Skeleton占位
- 按钮加载: Spin图标
- 页面加载: 居中Spin

#### 消息提示
- 成功: 绿色Toast，右上角
- 错误: 红色Toast，右上角
- 警告: 黄色Toast，右上角
- 确认: Modal对话框

## 六、API接口文档

> 📖 **完整 API 文档**: 查看 [API文档.md](./API文档.md) 获取详细的接口说明、请求示例和最佳实践

系统提供 **60+ 个 API 端点**，涵盖 9 个功能模块：

### 6.1 配置管理 API (5 个接口)

- `GET /api/config/active` - 获取当前配置
- `POST /api/config` - 保存配置（支持 DeepSeek、Gemini、Ollama）
- `POST /api/config/test` - 测试 API 连接
- `GET /api/config/ollama/models` - 获取 Ollama 模型列表
- `POST /api/config/ollama/test` - 测试 Ollama 连接

### 6.2 关键词蒸馏 API (13 个接口)

- `POST /api/distillation` - 执行蒸馏
- `GET /api/distillation/history` - 获取历史
- `GET /api/distillation/keywords` - 获取关键词列表
- `GET /api/distillation/results` - 获取结果列表（带筛选）
- `GET /api/distillation/stats` - 获取使用统计
- `GET /api/distillation/recommended` - 获取推荐结果
- `GET /api/distillation/:id` - 获取详情
- `DELETE /api/distillation/:id` - 删除记录
- `PATCH /api/distillation/:id` - 更新关键词
- `DELETE /api/distillation/topics` - 批量删除话题
- `GET /api/distillation/:id/usage-history` - 获取使用历史
- `POST /api/distillation/:id/reset-usage` - 重置使用统计
- `POST /api/distillation/repair-usage-stats` - 修复使用统计

### 6.3 话题管理 API (3 个接口)

- `GET /api/topics/:distillationId` - 获取话题列表
- `PUT /api/topics/:id` - 编辑话题
- `DELETE /api/topics/:id` - 删除话题

### 6.4 文章管理 API (5 个接口)

- `POST /api/articles/generate` - 生成文章（支持知识库引用）
- `GET /api/articles` - 获取文章列表（支持分页和任务筛选）
- `GET /api/articles/:id` - 获取文章详情
- `PUT /api/articles/:id` - 更新文章
- `DELETE /api/articles/:id` - 删除文章

### 6.5 文章生成任务 API (8 个接口)

- `POST /api/article-generation/tasks` - 创建任务
- `GET /api/article-generation/tasks` - 获取任务列表
- `GET /api/article-generation/tasks/:id` - 获取任务详情
- `GET /api/article-generation/tasks/:id/diagnose` - 诊断任务
- `POST /api/article-generation/tasks/:id/retry` - 重试任务
- `DELETE /api/article-generation/tasks/:id` - 删除任务
- `POST /api/article-generation/tasks/batch-delete` - 批量删除任务
- `DELETE /api/article-generation/tasks` - 删除所有任务

### 6.6 文章设置 API (5 个接口)

- `GET /api/article-settings` - 获取设置列表
- `POST /api/article-settings` - 创建设置
- `GET /api/article-settings/:id` - 获取设置详情
- `PUT /api/article-settings/:id` - 更新设置
- `DELETE /api/article-settings/:id` - 删除设置

### 6.7 转化目标 API (5 个接口)

- `GET /api/conversion-targets` - 获取目标列表
- `POST /api/conversion-targets` - 创建目标
- `GET /api/conversion-targets/:id` - 获取目标详情
- `PUT /api/conversion-targets/:id` - 更新目标
- `DELETE /api/conversion-targets/:id` - 删除目标

### 6.8 企业图库 API (8 个接口)

- `GET /api/gallery/albums` - 获取相册列表
- `POST /api/gallery/albums` - 创建相册
- `GET /api/gallery/albums/:id` - 获取相册详情
- `PATCH /api/gallery/albums/:id` - 更新相册
- `DELETE /api/gallery/albums/:id` - 删除相册
- `POST /api/gallery/albums/:albumId/images` - 上传图片
- `GET /api/gallery/images/:id` - 获取图片
- `DELETE /api/gallery/images/:id` - 删除图片

### 6.9 企业知识库 API (9 个接口)

- `GET /api/knowledge-bases` - 获取知识库列表
- `POST /api/knowledge-bases` - 创建知识库
- `GET /api/knowledge-bases/:id` - 获取知识库详情
- `PATCH /api/knowledge-bases/:id` - 更新知识库
- `DELETE /api/knowledge-bases/:id` - 删除知识库
- `POST /api/knowledge-bases/:id/documents` - 上传文档
- `GET /api/knowledge-bases/documents/:id` - 获取文档详情
- `DELETE /api/knowledge-bases/documents/:id` - 删除文档
- `GET /api/knowledge-bases/:id/documents/search` - 搜索文档

### API 使用示例

详细的请求/响应格式、curl 示例、JavaScript 代码示例和最佳实践，请参考 [API文档.md](./API文档.md)。

## 七、部署方案

### 7.1 开发环境

```bash
# 安装依赖
npm run install:all

# 配置环境变量
cp .env.example .env
# 编辑 .env 文件

# 数据库迁移
cd server && npm run db:migrate

# 启动开发服务器
npm run dev
```

### 7.2 生产环境部署

#### 推荐方案：腾讯EdgeOne云部署

**架构优势：**
- ✅ 全球CDN加速，访问速度快
- ✅ 自动DDoS防护和WAF安全防护
- ✅ 智能路由和负载均衡
- ✅ 免费SSL证书，自动HTTPS
- ✅ 按需付费，成本可控
- ✅ 与腾讯云生态无缝集成

**部署架构：**
```
EdgeOne边缘网络 (CDN + 安全防护)
    ↓
前端静态资源 (COS对象存储)
    ↓ API请求
后端应用服务器 (腾讯云CVM)
    ↓
云数据库 PostgreSQL
    ↓
外部AI服务 (DeepSeek/Gemini/Ollama)
```

**详细部署步骤：**
请参考 [腾讯EdgeOne部署方案](./腾讯EdgeOne部署方案.md)

#### 前端部署
```bash
cd client
npm run build
# 将 dist 目录上传到腾讯云COS
# 配置EdgeOne CDN加速
```

#### 后端部署
```bash
cd server
npm run build
# 部署到腾讯云服务器
# 使用PM2进程管理
# 配置Nginx反向代理
npm start
```

#### 数据库
- 使用腾讯云数据库PostgreSQL（推荐）
- 或自建PostgreSQL 12+
- 配置连接字符串
- 执行数据库迁移

### 7.3 环境变量

```env
# 数据库
DATABASE_URL=postgresql://user:password@host:5432/database

# AI API
DEEPSEEK_API_KEY=sk-xxx
GEMINI_API_KEY=xxx

# Ollama配置（可选）
OLLAMA_BASE_URL=http://localhost:11434

# 服务器
PORT=3000
NODE_ENV=production

# CORS配置（EdgeOne域名）
FRONTEND_URL=https://your-domain.com
```

### 7.4 云平台适配性

**✅ 适合EdgeOne的架构特性：**
1. **前后端分离** - 前端CDN加速，后端独立部署
2. **RESTful API** - 标准HTTP接口，易于边缘加速
3. **无状态设计** - 便于水平扩展和负载均衡
4. **静态资源构建** - Vite构建的SPA，完美适配CDN
5. **PostgreSQL数据库** - 可使用腾讯云数据库服务

**部署文档：**
- [腾讯EdgeOne部署方案](./腾讯EdgeOne部署方案.md) - EdgeOne云平台详细部署指南
- [部署指南](./部署指南.md) - 通用部署方案和最佳实践

## 八、安全考虑

### 8.1 API密钥安全
- API密钥加密存储
- 不在前端暴露密钥
- 使用环境变量管理敏感信息

### 8.2 输入验证
- 所有用户输入进行验证
- 防止SQL注入
- 防止XSS攻击

### 8.3 访问控制
- 可扩展用户认证系统
- API请求限流
- CORS配置

## 九、未来扩展

### 9.1 功能扩展
- 用户系统和权限管理
- 文章版本管理
- 批量关键词蒸馏
- 文章质量评分
- 导出为多种格式（PDF、Word等）

### 9.2 技术优化
- Redis缓存
- 消息队列处理长任务
- 微服务架构
- Docker容器化
- CI/CD自动化部署

### 9.3 AI能力增强
- 支持更多AI模型
- 自定义提示词模板
- 文章风格定制
- 多语言支持
