# 腾讯云服务器镜像选择指南

> 针对 GEO 优化系统的最佳镜像选择

## 🏆 推荐镜像

### 首选：Ubuntu 22.04 LTS ⭐⭐⭐⭐⭐

```
镜像名称：Ubuntu Server 22.04 LTS 64位
版本：22.04
架构：x86_64
支持期限：2027年4月
```

**选择理由：**
1. ✅ 最新的 LTS 版本，支持时间最长
2. ✅ Node.js 18/20 原生支持最好
3. ✅ PostgreSQL 14+ 原生支持
4. ✅ Puppeteer 依赖最容易安装
5. ✅ 所有软件都有最新稳定版本
6. ✅ 社区支持最好，问题最容易解决

**适合场景：**
- ✅ 新项目部署（推荐）
- ✅ 追求最新技术栈
- ✅ 需要长期支持

### 备选：Ubuntu 20.04 LTS ⭐⭐⭐⭐⭐

```
镜像名称：Ubuntu Server 20.04 LTS 64位
版本：20.04
架构：x86_64
支持期限：2025年4月
```

**选择理由：**
1. ✅ 更成熟稳定，经过充分验证
2. ✅ 兼容性最好
3. ✅ 问题解决方案最多
4. ✅ 所有依赖都有稳定版本
5. ✅ 社区支持最好

**适合场景：**
- ✅ 追求极致稳定
- ✅ 已有 Ubuntu 20.04 经验
- ✅ 需要最大兼容性

---

## 📊 镜像详细对比

### Ubuntu 22.04 LTS vs Ubuntu 20.04 LTS

| 特性 | Ubuntu 22.04 | Ubuntu 20.04 | 说明 |
|------|--------------|--------------|------|
| 支持期限 | 2027年4月 | 2025年4月 | 22.04 更长 |
| Node.js 18 | ✅ 原生支持 | ✅ 原生支持 | 都支持 |
| PostgreSQL | 14+ | 12+ | 22.04 更新 |
| Redis | 6.2+ | 5.0+ | 22.04 更新 |
| Nginx | 1.18+ | 1.18+ | 相同 |
| Puppeteer | ✅ 完美支持 | ✅ 完美支持 | 都支持 |
| 稳定性 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 20.04 更稳定 |
| 社区支持 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 都很好 |
| 推荐度 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | 都推荐 |

**结论：** 两者都很好，22.04 更新，20.04 更稳定

---

## ❌ 不推荐的镜像

### CentOS ⭐⭐⭐

**问题：**
1. ❌ CentOS 8 已停止维护（2021年12月31日）
2. ❌ CentOS Stream 不稳定，不适合生产环境
3. ❌ yum/dnf 包管理比 apt 复杂
4. ❌ Node.js 安装需要额外配置
5. ❌ Puppeteer 依赖安装非常麻烦
6. ❌ 社区支持逐渐减少

**如果必须用 CentOS：**
- 选择 CentOS 7（支持到 2024年6月30日）
- 或者选择 Rocky Linux / AlmaLinux（CentOS 替代品）

### OpenCloudOS ⭐⭐

**问题：**
1. ❌ 社区太小，文档少
2. ❌ 遇到问题难以找到解决方案
3. ❌ 兼容性未经充分验证
4. ❌ 不确定 Node.js/Puppeteer 支持情况
5. ❌ 主要针对腾讯云内部优化

**适合场景：**
- 仅适合腾讯云特定场景
- 不推荐新手使用

### Debian ⭐⭐⭐⭐

**不是不好，但不如 Ubuntu：**
1. ⚠️ 软件版本较旧（追求稳定）
2. ⚠️ 社区比 Ubuntu 小
3. ⚠️ 某些新特性支持较慢
4. ⚠️ 文档不如 Ubuntu 全面

**适合场景：**
- 追求极致稳定
- 有 Debian 经验
- 不需要最新软件

---

## 🚀 快速安装对比

### Ubuntu 安装（推荐）✅

```bash
# 1. 更新系统（1分钟）
apt update && apt upgrade -y

# 2. 安装 Node.js 18（2分钟）
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# 3. 安装 PostgreSQL（1分钟）
apt install -y postgresql postgresql-contrib

# 4. 安装 Redis（1分钟）
apt install -y redis-server

# 5. 安装 Nginx（1分钟）
apt install -y nginx

# 6. 安装 Puppeteer 依赖（2分钟）
apt install -y \
  chromium-browser \
  fonts-liberation \
  libasound2 \
  libatk-bridge2.0-0 \
  libatk1.0-0 \
  libcups2 \
  libdbus-1-3 \
  libdrm2 \
  libgbm1 \
  libgtk-3-0 \
  libnspr4 \
  libnss3 \
  libxcomposite1 \
  libxdamage1 \
  libxrandr2 \
  xdg-utils

# 7. 安装 PM2（1分钟）
npm install -g pm2

# 总耗时：约 10 分钟
```

### CentOS 安装（复杂）❌

```bash
# 1. 更新系统（2分钟）
yum update -y

# 2. 安装 Node.js 18（3分钟，需要额外配置）
curl -fsSL https://rpm.nodesource.com/setup_18.x | bash -
yum install -y nodejs

# 3. 安装 PostgreSQL（5分钟，需要添加仓库）
yum install -y https://download.postgresql.org/pub/repos/yum/reporpms/EL-8-x86_64/pgdg-redhat-repo-latest.noarch.rpm
yum install -y postgresql14-server
postgresql-14-setup initdb
systemctl enable postgresql-14
systemctl start postgresql-14

# 4. 安装 Redis（3分钟，需要 EPEL）
yum install -y epel-release
yum install -y redis
systemctl enable redis
systemctl start redis

# 5. 安装 Nginx（2分钟）
yum install -y nginx
systemctl enable nginx
systemctl start nginx

# 6. 安装 Puppeteer 依赖（10分钟，非常麻烦）
yum install -y chromium chromium-headless \
  liberation-fonts liberation-sans-fonts \
  liberation-serif-fonts liberation-mono-fonts \
  nss atk at-spi2-atk cups-libs gtk3 \
  libXcomposite libXcursor libXdamage libXext \
  libXi libXrandr libXScrnSaver libXtst \
  pango alsa-lib

# 7. 安装 PM2（1分钟）
npm install -g pm2

# 总耗时：约 25 分钟，且容易出错
```

**结论：** Ubuntu 安装时间是 CentOS 的 40%，且更简单

---

## 💻 服务器配置推荐

### 最低配置（测试环境）

```
CPU：2核
内存：2GB
硬盘：40GB SSD
带宽：1Mbps

价格：约 ¥50-80/月
适合：开发测试
```

### 推荐配置（生产环境）

```
CPU：2核
内存：4GB
硬盘：60GB SSD
带宽：5Mbps

价格：约 ¥100-150/月
适合：小型生产环境
```

### 高性能配置（大流量）

```
CPU：4核
内存：8GB
硬盘：100GB SSD
带宽：10Mbps

价格：约 ¥300-500/月
适合：大流量生产环境
```

---

## 📋 购买步骤

### 1. 登录腾讯云控制台

https://console.cloud.tencent.com/

### 2. 选择轻量应用服务器

https://console.cloud.tencent.com/lighthouse/instance/index

### 3. 选择配置

```
地域：选择离你最近的（如广州、上海）
镜像：Ubuntu Server 22.04 LTS 64位
套餐：2核4GB 或 4核8GB
时长：按需选择（建议先买1个月测试）
```

### 4. 配置安全组

```
开放端口：
- 22 (SSH) - 仅允许你的 IP
- 80 (HTTP)
- 443 (HTTPS)
- 其他端口全部关闭
```

### 5. 购买并启动

点击"立即购买"，等待服务器启动（约1-2分钟）

---

## 🔧 首次登录配置

### 1. 获取服务器信息

```bash
# 在腾讯云控制台获取
公网 IP：123.456.789.0
用户名：ubuntu（Ubuntu 系统）或 root（其他系统）
密码：在控制台重置密码
```

### 2. SSH 连接

```bash
# macOS/Linux
ssh ubuntu@123.456.789.0

# 或使用腾讯云控制台的 Web Shell
```

### 3. 更新系统

```bash
# 更新软件包列表
sudo apt update

# 升级所有软件包
sudo apt upgrade -y

# 重启（如果需要）
sudo reboot
```

### 4. 创建部署用户（可选）

```bash
# 创建用户
sudo adduser deploy

# 添加到 sudo 组
sudo usermod -aG sudo deploy

# 切换到新用户
su - deploy
```

---

## 🎯 最终建议

### 新手推荐

```
镜像：Ubuntu 22.04 LTS
配置：2核4GB
原因：
- 最简单易用
- 社区支持最好
- 遇到问题容易解决
- 安装软件最方便
```

### 追求稳定

```
镜像：Ubuntu 20.04 LTS
配置：2核4GB
原因：
- 更成熟稳定
- 兼容性最好
- 问题解决方案最多
```

### 预算充足

```
镜像：Ubuntu 22.04 LTS
配置：4核8GB
原因：
- 性能更好
- 可以运行更多服务
- 未来扩展性好
```

---

## 📚 相关文档

- [腾讯云部署源代码保护计划.md](./腾讯云部署源代码保护计划.md)
- [代码保护完成报告.md](./代码保护完成报告.md)

---

## ❓ 常见问题

### Q1: Ubuntu 22.04 和 20.04 选哪个？

**A:** 
- 新项目 → Ubuntu 22.04（支持更长）
- 追求稳定 → Ubuntu 20.04（更成熟）
- 两者都很好，看个人偏好

### Q2: 为什么不推荐 CentOS？

**A:**
- CentOS 8 已停止维护
- CentOS Stream 不稳定
- 安装软件更复杂
- 社区支持减少

### Q3: OpenCloudOS 怎么样？

**A:**
- 社区太小，文档少
- 兼容性未知
- 不推荐新手使用
- 仅适合腾讯云特定场景

### Q4: 2核2GB 够用吗？

**A:**
- 测试环境：够用
- 生产环境：建议 2核4GB
- 大流量：建议 4核8GB

### Q5: 需要购买域名吗？

**A:**
- 测试：不需要，用 IP 即可
- 生产：强烈建议购买域名
- 好处：更专业、可以配置 HTTPS

---

**总结：强烈推荐 Ubuntu 22.04 LTS 或 Ubuntu 20.04 LTS！**
