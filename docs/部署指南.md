# GEO优化系统 - 部署指南

## 一、部署概述

本指南将帮助您将GEO优化系统部署到生产环境。

### 部署架构

```
┌─────────────────────────────────────────────┐
│              负载均衡 / CDN                  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│           前端静态资源服务器                 │
│        (Nginx / Vercel / Netlify)           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│              后端应用服务器                  │
│         (Node.js + Express)                 │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│            PostgreSQL 数据库                 │
└─────────────────────────────────────────────┘
```

## 二、前端部署

### 方案一：Nginx部署（推荐）

#### 1. 构建前端

```bash
cd client
npm run build
```

构建产物在 `client/dist` 目录。

#### 2. 安装Nginx

**Ubuntu/Debian:**
```bash
sudo apt update
sudo apt install nginx
```

**CentOS/RHEL:**
```bash
sudo yum install nginx
```

**macOS:**
```bash
brew install nginx
```

#### 3. 配置Nginx

创建配置文件 `/etc/nginx/sites-available/geo-system`:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /var/www/geo-system;
    index index.html;
    
    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript 
               application/x-javascript application/xml+rss 
               application/javascript application/json;
    
    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API代理
    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### 4. 部署文件

```bash
# 创建部署目录
sudo mkdir -p /var/www/geo-system

# 复制构建文件
sudo cp -r client/dist/* /var/www/geo-system/

# 设置权限
sudo chown -R www-data:www-data /var/www/geo-system
```

#### 5. 启用配置

```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/geo-system /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重启Nginx
sudo systemctl restart nginx
```

#### 6. 配置HTTPS（推荐）

使用Let's Encrypt免费SSL证书：

```bash
# 安装Certbot
sudo apt install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run
```

### 方案二：Vercel部署

#### 1. 安装Vercel CLI

```bash
npm install -g vercel
```

#### 2. 登录Vercel

```bash
vercel login
```

#### 3. 部署

```bash
cd client
vercel --prod
```

#### 4. 配置环境变量

在Vercel控制台配置：
- `VITE_API_URL`: 后端API地址

### 方案三：Netlify部署

#### 1. 创建 `netlify.toml`

```toml
[build]
  command = "cd client && npm run build"
  publish = "client/dist"

[[redirects]]
  from = "/api/*"
  to = "https://your-backend-api.com/api/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

#### 2. 部署

```bash
# 安装Netlify CLI
npm install -g netlify-cli

# 登录
netlify login

# 部署
netlify deploy --prod
```

## 三、后端部署

### 方案一：传统服务器部署（推荐）

#### 1. 准备服务器

**系统要求:**
- Ubuntu 20.04+ / CentOS 8+
- Node.js 18+
- PostgreSQL 12+
- 2GB+ RAM

#### 2. 安装依赖

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 安装PostgreSQL
sudo apt install postgresql postgresql-contrib

# 安装PM2
sudo npm install -g pm2
```

#### 3. 配置数据库

```bash
# 切换到postgres用户
sudo -u postgres psql

# 创建数据库和用户
CREATE DATABASE geo_system;
CREATE USER geo_user WITH ENCRYPTED PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE geo_system TO geo_user;
\q
```

#### 4. 部署应用

```bash
# 创建应用目录
sudo mkdir -p /var/www/geo-system
cd /var/www/geo-system

# 克隆代码（或上传代码）
git clone <your-repo-url> .

# 安装依赖
cd server
npm install --production

# 构建
npm run build
```

#### 5. 配置环境变量

创建 `/var/www/geo-system/server/.env`:

```env
NODE_ENV=production
PORT=3000

# 数据库
DATABASE_URL=postgresql://geo_user:your_password@localhost:5432/geo_system

# AI API
DEEPSEEK_API_KEY=your_deepseek_key
GEMINI_API_KEY=your_gemini_key

# 安全
JWT_SECRET=your_jwt_secret_here
```

#### 6. 运行数据库迁移

```bash
cd /var/www/geo-system/server
npm run db:migrate
```

#### 7. 使用PM2启动

创建 `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [{
    name: 'geo-system',
    script: './dist/index.js',
    instances: 2,
    exec_mode: 'cluster',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    },
    error_file: './logs/err.log',
    out_file: './logs/out.log',
    log_date_format: 'YYYY-MM-DD HH:mm:ss',
    merge_logs: true,
    autorestart: true,
    max_memory_restart: '1G'
  }]
};
```

启动应用：

```bash
# 创建日志目录
mkdir -p logs

# 启动
pm2 start ecosystem.config.js

# 保存PM2配置
pm2 save

# 设置开机自启
pm2 startup
```

#### 8. 配置防火墙

```bash
# 允许HTTP和HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 允许SSH
sudo ufw allow 22/tcp

# 启用防火墙
sudo ufw enable
```

### 方案二：Docker部署

#### 1. 创建Dockerfile

**后端Dockerfile** (`server/Dockerfile`):

```dockerfile
FROM node:18-alpine

WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY . .

# 构建
RUN npm run build

# 暴露端口
EXPOSE 3000

# 启动命令
CMD ["node", "dist/index.js"]
```

**前端Dockerfile** (`client/Dockerfile`):

```dockerfile
FROM node:18-alpine as builder

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

FROM nginx:alpine

COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

#### 2. 创建docker-compose.yml

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:14-alpine
    environment:
      POSTGRES_DB: geo_system
      POSTGRES_USER: geo_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped

  backend:
    build: ./server
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: postgresql://geo_user:${DB_PASSWORD}@postgres:5432/geo_system
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY}
      GEMINI_API_KEY: ${GEMINI_API_KEY}
    ports:
      - "3000:3000"
    depends_on:
      - postgres
    restart: unless-stopped

  frontend:
    build: ./client
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  postgres_data:
```

#### 3. 部署

```bash
# 创建.env文件
cat > .env << EOF
DB_PASSWORD=your_db_password
DEEPSEEK_API_KEY=your_deepseek_key
GEMINI_API_KEY=your_gemini_key
EOF

# 启动服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down
```

### 方案三：云平台部署

#### Railway部署

1. 访问 [Railway.app](https://railway.app)
2. 连接GitHub仓库
3. 添加PostgreSQL服务
4. 配置环境变量
5. 自动部署

#### Heroku部署

```bash
# 安装Heroku CLI
npm install -g heroku

# 登录
heroku login

# 创建应用
heroku create geo-system

# 添加PostgreSQL
heroku addons:create heroku-postgresql:hobby-dev

# 配置环境变量
heroku config:set DEEPSEEK_API_KEY=your_key
heroku config:set GEMINI_API_KEY=your_key

# 部署
git push heroku main
```

## 四、数据库优化

### 1. 性能优化

```sql
-- 创建索引
CREATE INDEX idx_distillations_keyword ON distillations(keyword);
CREATE INDEX idx_distillations_created ON distillations(created_at DESC);
CREATE INDEX idx_topics_distillation ON topics(distillation_id);
CREATE INDEX idx_articles_keyword ON articles(keyword);
CREATE INDEX idx_articles_created ON articles(created_at DESC);

-- 分析表
ANALYZE distillations;
ANALYZE topics;
ANALYZE articles;
```

### 2. 备份策略

**自动备份脚本** (`backup.sh`):

```bash
#!/bin/bash

BACKUP_DIR="/var/backups/geo-system"
DATE=$(date +%Y%m%d_%H%M%S)
DB_NAME="geo_system"

# 创建备份目录
mkdir -p $BACKUP_DIR

# 备份数据库
pg_dump $DB_NAME | gzip > $BACKUP_DIR/db_$DATE.sql.gz

# 删除30天前的备份
find $BACKUP_DIR -name "db_*.sql.gz" -mtime +30 -delete

echo "Backup completed: db_$DATE.sql.gz"
```

**设置定时任务**:

```bash
# 编辑crontab
crontab -e

# 每天凌晨2点备份
0 2 * * * /path/to/backup.sh
```

### 3. 恢复数据

```bash
# 解压备份
gunzip db_20240101_020000.sql.gz

# 恢复数据库
psql geo_system < db_20240101_020000.sql
```

## 五、监控和日志

### 1. PM2监控

```bash
# 查看状态
pm2 status

# 查看日志
pm2 logs

# 监控面板
pm2 monit

# Web监控
pm2 plus
```

### 2. Nginx日志

```bash
# 访问日志
tail -f /var/log/nginx/access.log

# 错误日志
tail -f /var/log/nginx/error.log
```

### 3. 应用日志

使用Winston或Pino记录日志：

```typescript
import winston from 'winston';

const logger = winston.createLogger({
  level: 'info',
  format: winston.format.json(),
  transports: [
    new winston.transports.File({ filename: 'error.log', level: 'error' }),
    new winston.transports.File({ filename: 'combined.log' })
  ]
});

if (process.env.NODE_ENV !== 'production') {
  logger.add(new winston.transports.Console({
    format: winston.format.simple()
  }));
}
```

## 六、安全加固

### 1. 系统安全

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 配置防火墙
sudo ufw enable
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# 禁用root登录
sudo nano /etc/ssh/sshd_config
# 设置: PermitRootLogin no
sudo systemctl restart sshd
```

### 2. 应用安全

**安装安全中间件**:

```bash
npm install helmet express-rate-limit
```

**配置安全头**:

```typescript
import helmet from 'helmet';
import rateLimit from 'express-rate-limit';

// 安全头
app.use(helmet());

// 限流
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15分钟
  max: 100 // 限制100个请求
});
app.use('/api/', limiter);

// CORS
app.use(cors({
  origin: process.env.FRONTEND_URL,
  credentials: true
}));
```

### 3. 数据库安全

```sql
-- 限制用户权限
REVOKE ALL ON DATABASE geo_system FROM PUBLIC;
GRANT CONNECT ON DATABASE geo_system TO geo_user;
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO geo_user;

-- 启用SSL连接
ALTER SYSTEM SET ssl = on;
```

## 七、性能优化

### 1. 前端优化

```javascript
// vite.config.ts
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom', 'react-router-dom'],
          'antd-vendor': ['antd', '@ant-design/icons'],
        }
      }
    },
    chunkSizeWarningLimit: 1000
  }
});
```

### 2. 后端优化

**连接池配置**:

```typescript
const pool = new Pool({
  max: 20,
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 2000,
});
```

**缓存配置**:

```typescript
import Redis from 'ioredis';

const redis = new Redis({
  host: 'localhost',
  port: 6379,
  password: 'your_password',
  db: 0
});

// 缓存中间件
async function cacheMiddleware(req, res, next) {
  const key = `cache:${req.url}`;
  const cached = await redis.get(key);
  
  if (cached) {
    return res.json(JSON.parse(cached));
  }
  
  next();
}
```

### 3. 数据库优化

```sql
-- 配置参数
ALTER SYSTEM SET shared_buffers = '256MB';
ALTER SYSTEM SET effective_cache_size = '1GB';
ALTER SYSTEM SET maintenance_work_mem = '64MB';
ALTER SYSTEM SET checkpoint_completion_target = 0.9;
ALTER SYSTEM SET wal_buffers = '16MB';
ALTER SYSTEM SET default_statistics_target = 100;
ALTER SYSTEM SET random_page_cost = 1.1;
ALTER SYSTEM SET effective_io_concurrency = 200;
ALTER SYSTEM SET work_mem = '4MB';
ALTER SYSTEM SET min_wal_size = '1GB';
ALTER SYSTEM SET max_wal_size = '4GB';

-- 重启PostgreSQL
sudo systemctl restart postgresql
```

## 八、故障排查

### 常见问题

#### 1. 前端无法访问后端

**检查项**:
- 后端服务是否运行
- 防火墙是否开放端口
- Nginx代理配置是否正确
- CORS配置是否正确

#### 2. 数据库连接失败

**检查项**:
- PostgreSQL是否运行
- 连接字符串是否正确
- 用户权限是否足够
- 防火墙是否允许连接

#### 3. API调用失败

**检查项**:
- API密钥是否正确
- 网络连接是否正常
- API额度是否用完
- 请求格式是否正确

## 九、维护计划

### 日常维护

- 每天检查日志
- 监控系统资源
- 检查备份状态

### 每周维护

- 清理旧日志
- 检查磁盘空间
- 更新依赖包

### 每月维护

- 数据库优化
- 安全更新
- 性能分析

## 十、总结

完成以上步骤后，您的GEO优化系统应该已经成功部署到生产环境。记得：

- ✅ 定期备份数据
- ✅ 监控系统状态
- ✅ 及时更新安全补丁
- ✅ 优化性能瓶颈
- ✅ 记录重要变更

祝您部署顺利！🚀
